---
date: 2026-05-03
title: x corner move
current_head: a67d4a2
agent: "Claude Sonnet 4.6"
session_type: exploration
topics: [system-design, architecture, tttt, planning]
---

# prompt 1: X wants to win

I think I have figured out how I wound up with so few boards in my 1987 training set. If X is trying to win, then I think it that it should make its first move in a corner. And since there is only one canonical board for X first move in corner, the recursive descent tree is much smaller.

First, check that X first move should be a corner from game theory literature, then add that constraint to the generator and see how many boards are then in the training set.

## response 1

Added `CORNERS = np.array([0, 2, 6, 8])` to `board.py` and modified `generate_o_to_move()` to restrict X's first move to corners only. On the first X ply the candidates are `CORNERS`; all subsequent X plies use all empty cells as before.

Results after regenerating:

- Training set: **258** canonical positions (down from 1987)
- Test set: **1652** non-canonical variants
- First-move canonical positions: **1** (`x../.../...`) — all four corners are D4-equivalent, so they collapse to a single canonical board, confirming the hypothesis about why the original set was small.

The corner-first constraint is consistent with standard tic-tac-toe game theory: a corner opening is the strongest first move for X and forces O into a narrower set of correct responses.

# prompt 2: 1987 is a year

My "1987" training set is from the exercise I did in 1987 as described in README.md. I had a small training set at the time because training was so slow. I cannot remember what additional constraints I had in addition to "X starts with a corner move".

Read the README and then speculate as to how I created a training set of about 20 board states. I'd like to try to replicate that experiment from 40 years ago.

## response 2

The README says "all possible board states after X had moved" and "about 20 canonical states." With corner-first alone we get 258; something else must have constrained the tree further.

**Hypothesis: X always plays its single minimax-best move; O explores all legal moves.**

This means the tree never branches on X's choices — at every X turn there is exactly one move (the optimal one). Only O's responses cause branching. This is consistent with how someone hand-coding a training set would work: you'd trace the game tree from X's perspective, always taking the best move, and record every resulting O-to-move position O has to learn.

To verify, I added a quick experiment function that:
1. Forces X's first move to cell 0 (the canonical corner)
2. At every subsequent X turn calls `minimax()` and takes its single `best_move`
3. Explores all legal O responses (no pruning on O)
4. Canonicalizes each resulting board and deduplicates

Result: **24 canonical O-to-move positions** — consistent with "about 20":

| X pieces on board | canonical positions |
|---|---|
| 1 (X's 1st move) | 1 |
| 2 (X's 2nd move) | 6 |
| 3 (X's 3rd move) | 12 |
| 4 (X's 4th move) | 3 |
| 5 (X's 5th move) | 2 |
| **total** | **24** |

The full list of 24 boards (lexically-greatest canonical form):

```
x../.../...
x.x/.../.o.   xx./.o./...   xo./.../x..   xo./x../...   o.x/.../..x   o.x/..x/...
xo./xx./.o.   xo./xxo/...   xox/.x./.o.   xox/ox./...   o../.xo/.xx   o.x/..o/x.x
o.x/.xx/.o.   oxx/.x./..o   oxx/.xo/...   oxx/.o./..x   oxo/.../xx.   oox/.xx/...
xox/oxx/.o.   oxx/xoo/..x   oox/.xx/.xo
oxx/xoo/xox   oxo/xox/xox
```

**Replication plan:** add a `generate_o_to_move_x_optimal()` function to `board.py` that implements the same logic, regenerate the training set with it, and verify we get 24 records.

prompt 3: X plays optimally

That was it, my old training set did indeed only use optimal X moves. Is it possible for O to win in that circumstance or just force a draw?

Update the board generator to use only optimal X moves and remember that the test.tsv should contain all the non-canonical states generated.

As with your experiment above there should be 24 boards in the training set.

## response 3

**Can O win when X plays optimally?** No. Checking the minimax score of every training position with O to move yields only scores 0 (draw) and +1 (X wins). Score -1 (O wins) never appears. This is consistent with tic-tac-toe game theory: with perfect play from both sides the game is a draw, and if X plays optimally O cannot do better than draw.

**Generator update:** replaced the branching corner-first logic in `generate_o_to_move()` with X-optimal logic:
- First X move: always cell 0 (the canonical corner representative — no branching)
- Every subsequent X move: call `minimax()` and play the single best move (no branching)
- O: explores all legal responses as before

Results:
- Training set: **24** canonical O-to-move positions ✓
- Test set: **148** non-canonical D4 variants
